{
  "name": "recaptcha-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "nodemon server.js",
    "test": "NODE_ENV=test node --test",
    "railway:healthcheck": "curl -s http://localhost:$PORT/health"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.3",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "railway": {
    "startTimeout": 30000,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 5000
  }
}
